1- Ela permite que seja recuperada as informações  de um formulário definido com método POST.

2- Corretas: a e d
exemplo a - $pagina = $_POST['pagina'];
exemplo d - $nome = $_GET['nome'];
   Erradas: b e c
para iniciar um bloco PHP necessitamos <?php //code?>
Quando queremos resgatar um valor de campo input do html usamos o atributo NAME

3- Ambos fazem a inclusão de arquivos na página. A diferença é que o include, ao encontrar erro, gera um alerta de erro e renderiza a página enquanto o require, gera um alerta de erro e não renderiza a página.

4- x=640
   y=17280
   z=668
   a=668
